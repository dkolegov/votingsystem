# ===============================
# = DATA SOURCE
# ===============================
# Set here configurations for the MySQL database connections
# Connection url for the database "votingsystem"
spring.datasource.url = jdbc:h2:mem:test;MODE=PostgreSQL;DB_CLOSE_ON_EXIT=FALSE
# Connection url for the database "security"
spring.datasource.serurity.url = jdbc:h2:mem:test;MODE=PostgreSQL;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name = org.h2.Driver
spring.jpa.hibernate.ddl-auto = create-drop
spring.jpa.database = H2
spring.jpa.show-sql = true
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.H2Dialect
spring.datasource.username = test
spring.datasource.password = 
jdbc.initLocation=db/h2/initDB.sql
jdbc.dataLocation=db/h2/populateDB.sql

# for test use, not production use
jdbc.security.initLocation=db/h2/initSecurityDB.sql
jdbc.security.dataLocation=db/h2/populateSecurityDB.sql